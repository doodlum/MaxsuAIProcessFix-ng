[Settings]

EnableUpdateAfterWaiting = 1  		
;Set if enable update npc position after waiting, set value to 1 is enable, 0 is disable.

EnableUpdateAfterSleeping = 1		
;Set if enable update npc position after sleeping, set value to 1 is enable, 0 is disable.

EnableUpdateAfterFastTravel = 1		
;Set if enable update npc position after fast travel, set value to 1 is enable, 0 is disable. Disabled on VR.

MinimumTriggerHours = 2			
;The minimum lasting hours of a activity to trigger the npc AI position update function, modifiable value range is from 2 hours to 6 hours.

EnableUpdateModAddNPCs = 1
;Set if enable update mod added NPCs, set value to 1 is enable, 0 is disable.